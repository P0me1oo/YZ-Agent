# Xray REALITY 防盗用模式

## 背景

REALITY 服务端在读到客户端首包之前，就会先拨通伪装目标（面板里的“伪装站点”，配置里是 `dest`）。凡是认证不通过的连接——包括握手用了别的 TLS 域名——都会被原样双向转发到那个伪装目标。

伪装站点通常指向一个 CDN。于是任何人只要把 TLS 握手里的域名换成同一 CDN 上的其它站点，就能借这台节点访问它，节点等于对外白送了一个通用 TLS 转发入口：流量记在节点账上，来源看起来也是节点。

防盗用模式把伪装回源改接到一个只监听本机的专用入口，由它按伪装域名白名单放行，其余流量直接阻断。

## 开关位置

开关在面板的节点编辑页，协议为 VLESS 或 Trojan、安全性选择 Reality 时出现，名称为“防盗用模式”，默认关闭。面板把它随 REALITY 配置一起下发给节点（下发字段 `tls_settings.anti_abuse`），Node 收到后自动改写 Xray 配置，不需要在节点上写任何配置。

只有同时满足下面三条时才会生效，任何一条不满足都保持原来的行为：

- 节点内核是 Xray；
- 节点启用了 REALITY；
- 伪装域名列表（面板的“伪装站点域名”，配置里是 `serverNames`）至少有一个域名。

sing-box 内核没有对应实现，该开关对 sing-box 节点无效。

## 生效后的配置变化

Node 生成 Xray 配置时会做三件事：

1. 把节点入口 REALITY 的回源地址改成 `127.0.0.1:<专用端口>`；
2. 增加一个 `dokodemo-door` 入口，只监听 `127.0.0.1`，开启 TLS 嗅探；
3. 在路由表最前面插入两条规则：来自该入口且 TLS 域名命中白名单的走直连，其余一律阻断。

嗅探只改写目标地址，端口由该入口固定，取原伪装目标的端口（没写端口时按 443）。所以命中白名单后，连接按客户端实际请求的 TLS 域名回源，端口不变。

专用端口在 `40000-59999` 内按节点端口推算一个固定起点，再逐个试探可用端口，并避开节点自身监听的端口。同一节点重启后通常落在同一个端口，便于排查。

两条防盗用规则固定排在所有面板路由规则之前，面板上配置的通用路由规则不会把它们绕开。

REALITY 回源使用的是系统直连，不经过 Xray 路由，因此内置的禁止访问本机网段规则不会挡住这条回源链路。

## 副作用

关闭状态下，用错域名探测的人会拿到伪装站点的真实响应；打开后，这类连接会被直接断开。也就是说伪装的“像不像”会有变化：主动探测者能观察到，用白名单里的域名握手有回应，换个域名就断。这是拿一部分伪装表现换掉被当成公共转发入口的风险，是否接受由你决定。

其它影响：

- 伪装域名列表留空时，开关会被忽略并在日志中给出提示，回源地址保持原样。
- 伪装域名填错时开关照常生效，但没有域名能命中白名单，伪装回源会全部被阻断。开启前先确认列表填写正确。
- 阻断的连接不会有任何回包，探测方看到的是连接被重置。
- 白名单按完整域名精确匹配，不支持通配符，与 REALITY 内核自身比对 `serverNames` 的口径一致。

## 验证

面板保存后重启节点服务，或等待节点下一次拉取配置：

```bash
yz-agent service restart
```

Xray 配置由 Node 在内存中生成并直接交给内核，磁盘上没有可查看的 `config.json`。是否生效看节点日志中的 `REALITY 防盗用已启用`，其中会打印本机入口地址、放行的域名和回源端口。

实际效果可以这样核对：

- 用正常客户端连接节点，行为不变；
- 用浏览器或 `curl` 以伪装域名访问节点端口，应能拿到伪装站点的响应；
- 换成同一 CDN 上的其它域名访问节点端口，连接应被直接断开。
